# 0.0.75 Community Review Correction

## Reported Blocker

The default-branch manifest declared 0.0.74, but that release was a draft.
A draft is not downloadable by the Community scanner. Having a tag alone is
insufficient. This release publishes a new matching version, 0.0.75.

The plugin ID remains `docferry`; the separate MediaFerry product must not
change this identity or share its installation directory.

## Skill Review

Local verification: 222 tests passed; lint, build and public release checks
passed. The exact 0.0.75 package passed real Obsidian 1.13.7 settings navigation
and persisted-default checks (1.12.7 Electron installer). Its runtime and CSS
hashes equal the previously verified 0.0.74 candidate. No real account or
subscription was changed.

- Stable ID, MIT license, desktop-only flag and minimum host remain unchanged.
- Root/plugin manifests, packages, lockfiles and version maps must agree.
- No new runtime source changes from the tested 0.0.74 settings correction.
- Privacy and Optional payments disclosures remain applicable.
- Re-run lint, 222 client tests, bundle, source scan and dependency audits.
- Exercise the exact candidate in isolated real Obsidian settings windows.
- Tag exactly `0.0.75`; CI builds, attests and publishes the three assets.
- Run `node scripts/verify-published-release.mjs` after publication. It refuses
  draft/prerelease, mismatched ID/version, missing assets or differing hashes.
- No official dashboard submission or approval is implied by this checklist.

The [0.0.74 record](0.0.74.md) is historical candidate evidence, not the current
publication policy. New GitHub releases are automatically reviewed under the
[official policy](https://obsidian.md/blog/future-of-plugins/).
