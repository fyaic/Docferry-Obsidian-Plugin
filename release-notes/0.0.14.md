DocFerry 0.0.14

This release updates public release metadata for Obsidian Community review. There are no user-facing behavior changes.

**Release metadata:**

- Bumps the root and plugin manifests, npm metadata, and versions maps to `0.0.14`.
- Fixes the documented minimum Obsidian version to `1.13.0`, matching the manifest.
- Updates the plugin subdirectory README version to `0.0.14`.

**Build reproducibility:**

- Pins the plugin subdirectory's `obsidian`, `@codemirror/state`, and `@codemirror/view` development dependencies to exact versions.
- Keeps the bundle behavior unchanged.

**Compatibility:**

- Minimum Obsidian version remains `1.13.0`.
- Desktop-only support remains unchanged.

**Release assets:**

- `main.js`
- `manifest.json`
- `styles.css`
